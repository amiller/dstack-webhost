# archive

Parked, non-live artifacts (reversible; not deleted).

Superseded by the Paseo loop: the old smithers batch-runner artifacts —
`workflows/orchestrator.tsx`, `workflows/explore-rfc.tsx`, `workflows/run-*.log`, and
`smithers.db`. These are all UNTRACKED and physically live in the main checkout
(`~/projects/tee-daemon`), not on `origin/main`, so they were left in place rather than moved
from this worktree. Archive/delete them manually in the main checkout when convenient.
